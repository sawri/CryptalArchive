<!-- ARCHIVE LIST--------------------------------------------------------------------------------------------------------------------------------------->
<section id="archive" class="sec">

<div class="container">
    <div class="row justify-content-between">
        <!-- Archive -->
        <div class="col-md-2 pagelistblock">
            <div id="archivelist">
              <!-- links go here -->
                <%- include('navbtns'); -%>
                <hr>
                <p> Archive </p>
                <!-- dynamic archive list -->
                <% for (let i = 1; i <= pages; ++i) { %>
                  <a href="/pages/<%= i %>" class="comiclink"> <%= i %> </a><br>
                <% } %>
            </div>
        </div>
        <div class="col-lg-7 col-12 pageblock">
